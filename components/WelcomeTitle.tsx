import React, { FunctionComponent } from "react";
import { Text, View } from "react-native";

const WelcomeTitle: FunctionComponent = () => {
  return (
    <View className="my-5">
      <Text className="text-4xl font-semibold">Hello Jhon 👋</Text>
    </View>
  );
};

export default WelcomeTitle;
